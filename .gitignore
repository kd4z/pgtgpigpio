# Build output
build-aarch64/

# Debian package
*.deb

# Leftover staging tree (old package script ran from project dir)
pgtgpigpio_*/

# C object and library files
*.o
*.a
*.so
*.so.*

# CMake artifacts (if cmake is ever run in the project root)
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
Makefile
build/
